# Z-Function 
class Solution:
    def strStr(self, haystack: str, needle: str) -> int:
        k=haystack.find(needle)
        if(k>=0):
            return k
        else:
            return -1
 Python Code
 ********************************************************************************************************************************************************************
CPP CODE
#include<bits/stdc++.h>
using namespace std;
int main()
{
int count=0;

    int index=0;

    while(1){

        index=s.find(p);

                if (index >= 0 && index <= s.length() - 1) {

                  count++;

                  s[index] = '1';

                }

                else{

                    break;

                }

        }

    return count;
    }
